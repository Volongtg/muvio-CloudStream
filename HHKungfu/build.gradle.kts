// CloudStream plugin version. Increase this when the plugin changes.
version = 7

cloudstream {
    description = "Provider CloudStream cho HHKUNGFU"
    authors = listOf("Vo Long")
    status = 1
    tvTypes = listOf("Anime", "AnimeMovie", "TvSeries", "Movie")
    language = "vi"
    iconUrl = "https://hhkungfu.ee/favicon.ico"
}
